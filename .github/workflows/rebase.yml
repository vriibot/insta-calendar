name: rebase

on:
  push:
    branches: ['main']

permissions:
  contents: write

jobs:
  rebase:
    runs-on: [ ubuntu-latest ]

    concurrency:
      group: "rebase-deploy"
      cancel-in-progress: false

    steps:
      - name: Rebase
        run: |
          git checkout deploy
          git rebase master 
          git push origin deploy
